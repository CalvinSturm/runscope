{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability set for RunScope.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
